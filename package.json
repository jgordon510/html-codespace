{
  "name": "html-codespace",
  "version": "1.0.0",
  "description": "A boilerplate for plain html, javascript, and css in a codespace.",
  "main": "index.js",
  "scripts": {
    "dev": "live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
